# Pin Docker Engine API version so tests work against Docker Desktop on macOS,
# which now refuses requests below 1.44.
api.version=1.44

# Reuse containers across test classes within the same JVM (singleton pattern).
testcontainers.reuse.enable=true
