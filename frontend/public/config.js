// Replaced at container startup by entrypoint.sh.
// For local dev (npm run dev) edit authProvider here to match your backend:
//   'keycloak' (SSO) | 'native' (username+password) | 'none' (no auth, dev user)
window.__LILAC_CONFIG__ = {
  authProvider: 'keycloak',
};
